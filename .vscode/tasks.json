{
    "tasks": [
        {
            "type": "shell",
            "label": "Build vihtorizx",
            "command": "make",
            "args": [
                "CCFLAGS=-g"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}